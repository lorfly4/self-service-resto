<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof store !== 'undefined' ? store.name : 'Online Order' %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
        .colorful-bg {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
    </style>
</head>
<body class="bg-gray-50 text-gray-800 min-h-screen flex flex-col">

    <header class="colorful-bg text-white shadow-lg sticky top-0 z-50">
        <div class="container mx-auto px-4 py-4 flex justify-between items-center">
            <a href="<%= typeof store !== 'undefined' ? '/stores/' + store.slug : '/' %>" class="text-2xl font-bold tracking-tight">
                🍽️ <%= typeof store !== 'undefined' ? store.name : 'SelfService' %>
            </a>
            <% if (typeof store !== 'undefined') { %>
            <nav class="space-x-4">
                <a href="/stores/<%= store.slug %>" class="hover:text-gray-200 transition font-medium">Menu</a>
                <a href="/stores/<%= store.slug %>/cart" class="hover:text-gray-200 transition font-medium relative">
                    Cart
                </a>
                <a href="/stores/<%= store.slug %>/history" class="hover:text-gray-200 transition font-medium">History</a>
            </nav>
            <% } %>
        </div>
    </header>

    <main class="container mx-auto px-4 py-8 flex-grow">
